const home = require('./home-controller')
const users = require('./users-controller')
// const chats = require('./chats-controller')

module.exports = {
    home: home,
    user: users,
    // chats: chats
}